name: Repo Agent
on:
  workflow_dispatch:

jobs:
  run-agent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./  # <-- Fix: Points directly to the root of your current repository
        with:
          prompt-file: .agent.md
          issue-output: true
